# Base URL of the IFWYD backend API (routes add /api/... in code).
# Leave unset in development to use mock data + the dev-only fake login
# fallback (see src/providers/AuthProvider.tsx).
NEXT_PUBLIC_API_URL=http://127.0.0.1:5000
